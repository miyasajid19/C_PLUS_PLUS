// Find the minimum number of brackets that we need to remove
// to make the given bracket sequence balanced.
#include <iostream>
#include <cstdlib>
#include <stack>
#include <string>
using namespace std;
class Stacks
{
    char *arr;
    string str;

public:
    Stacks(string str, int size)
    {
        arr = new char[size];
        this->str = str;
    }
    
};
int main()
{
#ifndef ONLINE_JUDGE
    freopen("../../input.txt", "r", stdin);
    freopen("../../output.txt", "w", stdout);
#endif

    return EXIT_SUCCESS;
}